{
  "name": "@hzense/database",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test",
    "test": "vitest run --passWithNoTests",
    "test:migrations": "node test/require-integration-env.mjs && vitest run test/migration.integration.test.mjs test/topic-sync.integration.test.mjs && vitest run test/topic-sync-role.integration.test.mjs",
    "db:preflight:production": "node src/preflight.mjs",
    "db:migrate": "node src/production-migrate.mjs",
    "db:migrate:local": "node src/local-migrate.mjs",
    "db:verify:production": "node src/preflight.mjs && node src/verify.mjs"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  }
}
