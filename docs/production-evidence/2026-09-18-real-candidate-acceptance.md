# PR #112–#113 生产部署与真实资料候选验收

## 范围

本轮按用户要求执行生产部署确认与一次真实资料的私有候选验收。不进行独立事实核验执行器开发、正式 Signal 发布、数据库迁移、角色授权、密钥变更或数据源切换。

## 初始核对（历史记录）

- [PR #112](https://github.com/hzense/tech-intelligence-hub/pull/112) 已评审并合并，提交 `de8e7d591e3a747a95d51ba01d760e87590620f5`；本地 main 已同步。
- [main CI 35390971897](https://github.com/hzense/tech-intelligence-hub/actions/runs/35390971897) 成功。
- 对应 Production 部署 `dpl_DzKP8kgntBzHSxx9twVDeYTNLfiA` 已创建，当前核对仍为 INITIALIZING，尚不能记作上线完成。未取消或重复部署。
- 真实管理员已登录。线上生成尚未启用，原有 DeepSeek 私有候选验收 r1 显示测试证明有效；三个阶段均为 `deepseek/deepseek-v4.1-flash`／连接 r2。本轮未重跑能力测试。
- 只读核对 Production 非秘密配置：生成单批 `5000000` microUSD、UTC 每日 `10000000` microUSD，分别为 5／10 美元。未读取或下载数据库连接值、供应商密钥。

## 已确认的真实资料范围

拟使用 [NVIDIA 官方公告](https://blogs.nvidia.com/blog/runai/)，原文发表于 2024-04-24，记述 NVIDIA 签署收购 Run:ai 的最终协议，含 Run:ai 联合创始人兼 CEO Omri Geller 的具名表态。验收应保留“宣布拟收购”的事件阶段，不能改写为交易已完成或当日新事件。

用户已明确回复“确认”：仅将此文解析文本发送到已配置 OpenRouter／DeepSeek，临时开启后执行一次，不自动重试，结束恢复关闭，沿用 5／10 美元预算。该授权已获得，但不代表已产生模型调用或费用。

## 部署阻塞核对（2026-09-18 20:48 UTC）

- Vercel 接口与部署页面均显示 `dpl_DzKP8kgntBzHSxx9twVDeYTNLfiA` 为 INITIALIZING，来源为 main／`de8e7d5`，页面显示创建已超过 20 分钟。
- 页面展开 Build Logs 后仍显示 Loading；本轮构建日志连接器返回工具不可用，未取得可供诊断的构建错误。不能把初始化延迟定性为代码构建失败。
- 未取消部署、重复发起生产部署或调整环境变量；生成开关保持原关闭状态。未导入此公告、创建生成任务或调用模型。
- 新版本生产就绪前暂停后续验收，避免用旧版本证明新的 50／800／5 限制有效。若需取消此未完成部署并以相同提交重试，应独立确认；此前批准的预览部署重试不作为本次生产取消授权。

## 验收标准

1. 部署 Ready 且 hzense.com 使用本次代码，新的资料检查入口可见；专用数据库只读预检通过。
2. 公开资料只导入一个新批次，解析输出及定位可读取；检查来源 JSON 不超过 48000 字节，无静默截断。
3. 一次明确授权的真实模型调用，任务持久化成功；非空候选总数不超过 5。
4. 标题不超过 50、摘要不超过 800 个 Unicode 码点；标点、字母、空白均计入。
5. 事件日期为原文所述 2024-04-24，人物角色和组织关联有该事件证据；不能把文章作者当作事件参与人补位。
6. 核心断言和人物引用逐字对应解析片段，保留“拟收购”措辞；缺口不伪装为已核验。
7. 固定任务链接重新读取与脱敏运行日志一致；记录估算费用和 token 用量，不宣称等于实际账单。
8. 无正式 Signal 写入、无公开发布、无自动重试；验收结束恢复关闭并验证关闭配置生效。

## 当前结论

部署与数据库只读验收已通过。PR #113 五分钟版本上的独立授权单次验收已生成并持久化 1 条非空私有候选，长度、事件日期、人物与逐字引用检查通过；候选仍待审核及公开证据补齐，未发布。项目配置与生产运行时均已恢复生成关闭。早前失败／未知任务保留不变，具体见末尾续接记录；此前未通过、未调用模型、初始化中等描述均为当时状态。

## 重新部署与只读核验（2026-09-18 21:22 UTC）

- 原部署 `dpl_DzKP8kgntBzHSxx9twVDeYTNLfiA` 后续已 Ready，构建 29 秒。最初域名分配显示 Skipped，随后页面显示 Current Domains 包含 hzense.com；线上“检查生成资料（不调用 AI）”入口已经可见。初始化等待不等于构建失败。
- 两个 warning 定位分别为汇总行及同一组 Turborepo 环境变量未声明清单；日志显示 3 个任务成功、Deployment completed。未在本轮修改 `turbo.json`，不声称警告已解决。
- 用户澄清要求由助手重新部署后，于 21:16:30 UTC 从原部署发起一次 Production Redeploy，同一提交 `de8e7d5`，未勾选已有构建缓存，未修改环境变量。新部署为 [`dpl_FNDkb9N2LL4SAU3ewUUPKsxQzyzL`](https://vercel.com/zhenghu25-6909s-projects/tech-intelligence-hub-web/FNDkb9N2LL4SAU3ewUUPKsxQzyzL)。截至 21:22 UTC 仍为 INITIALIZING，尚未完成新部署验收；未取消或重复创建。
- [Vercel 官方事故](https://www.vercel-status.com/incidents/bwkmw4hmrgmk) 于 20:32 UTC 开始调查部署触发错误增多，20:56 UTC 更新为继续调查，影响 Build & Deploy（Builds）。时间与本次初始化停留重叠，属于可能相关的平台故障证据，不将其写成已确定的单次部署根因。
- 当前正式网站首页 HTTP 200，后台生成关闭。21:22 UTC 在 hzense.com 显式运行只读预检，Production 配置、专用凭据连接、TLS、真实身份与目标库、只读事务、最小权限六项全部通过；这是当前已上线版本的核验，不冒充尚在初始化的新部署结果。
- 未调用模型、未导入新资料、未发布信号。保留云端部署等待平台恢复，本轮结束时不承诺后台持续监控；新部署 Ready 后仍需重新核对生产域名与运行时。

## 部署验收与真实资料准备（2026-09-18 21:41 UTC，进行中）

- `dpl_FNDkb9N2LL4SAU3ewUUPKsxQzyzL` 已 Ready，域名包含 hzense.com，`aliasError=null`，提交仍为 `de8e7d5`。首页 HTTP 200，Google 管理员登录成功，资料检查入口可见。
- 新部署上的生成数据库只读预检六项全部通过。运行日志扫描发现登录请求 HTTP 200 伴随 Node `DEP0169 url.parse()` 弃用告警，未据此判定登录失败。
- 唯一获批 NVIDIA Run:ai 公告已导入批次 `3b654657-a6e0-4154-88fd-b8a6e48acea8`，显式处理一次后解析完成，页面报告临时原件已清理。解析为 72 个片段，包含导航／相关新闻；正文日期位于片段 5，协议公告位于片段 13，Omri Geller 表态及职务位于片段 19。模型质量验收须排除旁栏新闻及日期干扰。
- DeepSeek 私有候选验收 r1 仍显示证明有效，三个阶段均为 `deepseek/deepseek-v4.1-flash`／连接 r2，未重跑能力测试或修改配置。
- 按已确认授权，将 Production `HZENSE_SIGNAL_GENERATION_ENABLED` 从 `0` 临时保存为 `1`，未修改预算、密钥或权限；启用部署 `dpl_EvDLksHU11Wq7HN7umWaYQPkJKfm` 已创建，目前 INITIALIZING。尚未创建生成任务或调用模型。
- **收尾义务：** 仅一次私有候选生成，无重试、无发布；执行后将开关恢复 `0`，重新部署并确认运行时关闭。当前记录不是已完成验收。

## 单次真实资料验收结果（2026-09-18 21:55 UTC）

- 临时启用部署 `dpl_EvDLksHU11Wq7HN7umWaYQPkJKfm` 于 21:47:53 UTC Ready，随后通过 Promote 操作切换正式域名。hzense.com 工作台输入控件已启用；实际生成请求的运行日志也归属该部署，确认不是旧版本结果。
- 线上资料检查通过：72 个片段，来源 JSON **8,877 字节／48,000 字节上限**，解析版本 1。该检查未创建任务、预留预算或调用模型；正文未截断。
- 唯一请求 [`a78d4109-0c43-4e99-ad4b-d03ec11050fd`](https://hzense.com/admin/signal-generation/a78d4109-0c43-4e99-ad4b-d03ec11050fd) 于 21:52:19 UTC 创建，资料为 `38f7efa1-f6ca-401f-bc72-6200d99d8b89`，使用既有 DeepSeek Profile r1。仅点击一次执行，未重试。
- 固定任务页重新读取为 `status=failed`、`result=null`、`error_code=generation_sdk_error`。预留和保守记账均为 **244265 microUSD（0.244265 美元）**，不等于供应商实际账单。详情未提供 token 用量，不能记录为零。
- 运行日志：21:52:37 UTC 的 `POST /api/admin/signal-generation` 返回 HTTP 200，但业务结果为失败；provider 阶段 18,003ms，completion 阶段 61ms，截止均为 45,000ms。不能把 HTTP 200 当成生成成功，也没有证据将本次失败归因为超时、数据库连接、预算或原件导入。
- 当前分类器会把未识别的 SDK／输出处理异常统一归为 `generation_sdk_error`，没有保存具体安全子分类；现有证据不足以确定底层异常。下一步先补充不泄露正文／密钥的异常类型与输出完成原因诊断及模拟回归，再申请一次受控复验；本轮不额外调用模型。
- 没有生成可供验收的非空结果，50／800／5、事件日期、人物关联及逐字引用的真实输出质量均**未通过验收**，不是已证明这些规则有错；未写入正式 Signal、未公开发布。
- 已将 Production `HZENSE_SIGNAL_GENERATION_ENABLED` 恢复保存为 `0`，关闭配置部署 `dpl_6hDJXs2AqEsBhK8gh1scw4cW9eo5` 于 21:53:50 UTC 创建，同一提交 `de8e7d5`。截至本段记录时仍在排队，运行时关闭尚待核对；预算仍为每批 5／UTC 日 10 美元。

## 关闭状态恢复（2026-09-18 21:58 UTC）

- 新关闭配置部署 `6hDJXs2Aq` 显示 Queued，页面明确提示另一个构建正在进行；未取消其他构建或重复创建部署。
- 为缩短临时开启时间，已将相同提交 `de8e7d5`、此前通过验收且开关为 `0` 的 `dpl_FNDkb9N2LL4SAU3ewUUPKsxQzyzL` 重新 Promote 到正式域名。部署 API 确认 Ready、包含 hzense.com／www.hzense.com，`aliasError=null`。
- 重新进入 hzense.com 生成工作台，页面显示生成尚未启用，资料／配置选择、创建和执行相关控件禁用；原请求 ID 保留，没有再次调用 AI。**运行时关闭已确认，不只是环境变量保存成功。**
- 第 1 项部署／连接验收通过；第 2 项单次真实调用已执行但验收失败。无自动重试、无公开发布、未修改数据库权限或密码。新关闭配置部署仍待 Ready 与域名核对，不写成已完成；其配置同样为关闭，不需要再次开启生成。

## OpenRouter r5 单次复验（2026-09-18 22:21 UTC）

- 本地日期为 2026-09-19。用户明确允许将既有验收配置绑定当前 OpenRouter r5，并对同一 NVIDIA 公告执行一次私有候选生成，维持每批 5／UTC 日 10 美元预算，不发布、不自动重试，结束恢复关闭。
- AI 连接页面显示 r5 的基础连接、结构化输出及工具能力证明均通过；本轮复用现有证明，没有额外运行能力测试或读取密钥。既有“DeepSeek 私有候选验收 2026-09-18”保存为 Profile r2，刷新确认测试证明有效；三阶段均绑定连接 r5／`deepseek/deepseek-v4.1-flash`，提示词、温度 0.7、最大输出 2048 与无需工具要求均保持不变。
- 复用同一提交 `de8e7d5` 的 Ready 启用部署 `dpl_EvDLksHU11Wq7HN7umWaYQPkJKfm` 临时 Promote 至生产；未新建部署、改动项目保存的关闭环境变量、密钥或数据库权限。线上六项只读预检通过，资料检查仍为 72 片段、8,877 字节、解析版本 1。
- 新任务 [`b2febede-6d19-459e-bcb3-96837a72ed1c`](https://hzense.com/admin/signal-generation/b2febede-6d19-459e-bcb3-96837a72ed1c) 于 22:17:57 UTC 创建，资料 `38f7efa1-f6ca-401f-bc72-6200d99d8b89`、Profile `14b6589a-781d-4adb-99e9-c212b7eb4ed8` r2；仅点击一次执行，未重试旧任务或覆盖旧记录。
- 固定任务页读取得到 `status=unknown`、`error_code=generation_timeout`、`result=null`。对应启用部署的运行日志显示 provider 阶段 **45,002ms／45,000ms 截止**，completion 阶段 62ms，均记录 `unknown / generation_timeout`。HTTP 200 仅说明应用返回任务状态，不等于模型生成成功。
- 预留与保守记账均为 **244265 microUSD（0.244265 美元）**，不是供应商最终账单；无可核对的 token 用量，不能记作零。现有日志证明应用截止时间已触发，不能断言供应商最终没有生成、没有收费，或把排队／网络／模型推理中的某一个原因认定为根因。需按原请求对账，不得直接重复调用。
- 无候选结果可进行 50／800／5、事件时间、人物关系和原文引用验收；本次不算真实非空生成通过。没有正式 Signal 写入或公开发布。
- 收尾已将 Ready 的关闭部署 `dpl_6hDJXs2AqEsBhK8gh1scw4cW9eo5`（同一提交）Promote 至 hzense.com。新打开正式生成工作台，显示生成未启用、资料／配置选择与创建按钮均禁用，确认运行时已关闭；不是仅保存环境变量。保留新任务固定链接供管理员对账。
- 后续应先核对这一个未知请求的供应商记录，并补充不泄露正文和密钥的异常子分类／完成原因诊断，再评估输出 token 与截止时间是否适配；本轮未修改代码或擅自延长超时、提高预算、再次调用模型。

## 五分钟版本单次验收（2026-09-18 23:08 UTC）

- 本地日期为 2026-09-19。[PR #113](https://github.com/hzense/tech-intelligence-hub/pull/113) 已评审、合并并同步至 `515c6b6e49824cf92a59e5416ada94def783436f`；[合并后 CI 35403208198](https://github.com/hzense/tech-intelligence-hub/actions/runs/35403208198) 成功。接口 `maxDuration=300`，模型截止 285,000ms；原 7 分钟租约、预算和输出限制不变。
- 用户独立确认：临时启用生产生成，保存一个独立验收配置修订，仅将同一已批准 NVIDIA Run:ai 公告的解析文本发送给 OpenRouter／DeepSeek 一次；不重放旧未知任务、不发布，结束恢复关闭。预算每批 5／UTC 日 10 美元，未读取密钥、修改权限或运行迁移。
- 复用 OpenRouter r5 的有效能力证明，保存既有 Profile `14b6589a-781d-4adb-99e9-c212b7eb4ed8` 为 r3，三阶段均为 `deepseek/deepseek-v4.1-flash`，默认提示词、最大输出 2048、温度 0.7、无需工具要求不变；未额外运行能力测试。
- 临时启用部署 `dpl_H2XbWjLGpKXSMqNyZxBGRcvxiVtE` 于 23:01:22 UTC Ready，提交为 `515c6b6`。正式工作台六项只读预检通过；资料检查仍为 72 片段、8,877／48,000 字节、解析版本 1。
- 新任务 [`5f9c8bdb-00da-45af-86a7-1c84a9f38f65`](https://hzense.com/admin/signal-generation/5f9c8bdb-00da-45af-86a7-1c84a9f38f65) 于 23:02:17.923 UTC 创建，批次 `3b654657-a6e0-4154-88fd-b8a6e48acea8`、资料 `38f7efa1-f6ca-401f-bc72-6200d99d8b89`、Profile r3；仅执行一次。固定详情页独立读取为 `completed`、`error_code=null`，结果已持久化，而非仅浏览器即时展示。
- 对应该临时部署的运行日志记录 provider **41,957ms**、completion **70ms**，均为 `completed`，`timeout_ms=285000`。新截止配置已生效，但本次实际调用短于旧 45 秒截止，不能把此次成功解释为“必需更长等待”，也不能据此消除旧未知请求的对账义务。
- 生成 **1 条**候选，标题 **23／50**、摘要 **346／800** 个 Unicode 码点；日期为 **2024-04-24**，主体为 NVIDIA 宣布签署收购 Run:ai 的最终协议，未写成交易已完成或采集日新闻。人物为 **Omri Geller**，具名职务／组织为 Run:ai 联合创始人兼 CEO，未用文章作者补位。导航与 2026 年相关新闻未形成候选。
- 逐字比对已读取的解析内容：2 处日期证据、1 处人物证据、5 处主张证据均为对应片段原文子串，共 **8／8** 通过。候选保留 `classification=private`、`status=needs_review`、`issues=[needs_public_evidence]`。这只证明来自所选原文，尚不等于独立公开事实核验；正式人物组织关系及 Signal 发布未执行。
- 持久化用量为输入 **3.577K**、输出 **0.815K tokens**；预留与保守记账均为 **244265 microUSD（0.244265 美元）**，不是供应商最终账单。旧未知请求的费用没有释放、重算或抹除。
- 收尾先将项目 Production `HZENSE_SIGNAL_GENERATION_ENABLED` 保存为 `0`，待唯一请求完成后，将同一提交且关闭配置的 Ready 部署 `dpl_6exuTgPL34C5y83aJKk2kn7vBDrk` Promote 到 hzense.com。新打开正式工作台，五分钟说明仍在，生成未启用、资料选择与创建控件禁用，确认**运行时关闭**，不是只保存环境变量。环境备注更新为本次验收完成；候选固定链接保留给管理员查看。
- 结论：本轮真实非空私有候选的端到端单样本验收通过。没有自动重试、额外能力调用、正式 Signal 写入或公开发布；不宣称长期稳定性、自动采集、独立核验与自动发布已完成。后续正式开放手动生成应单独确认，候选发布前仍需审核及补证。

## 关闭开关后历史详情不可读：复核与修复（2026-09-19）

- 用户反馈看不到内容后，重新打开同一固定链接，管理员仍已登录，但详情显示“记录不存在、无权访问或生成服务尚未配置”。上段固定详情成功读取发生在关闭版本切换完成前；随后只核验了工作台禁用，未再次打开详情。因此本轮业务生成／持久化成功的证据仍成立，但当时没有证明关闭开关后历史详情可读。
- 原因定位为 `generationDetail`、`generationDashboard` 和数据库池均依赖包含生成启用开关的配置判定，关闭生成会连带拒绝历史读取，不是重新生成失败或已证实数据丢失。
- 本地已分离历史读取和生成门禁，复用连接目标与角色校验、会话 owner 过滤，历史读取显式只读且不触发过期状态修复；创建／执行／取消仍保留原门禁。详情新增可读候选卡片，保留折叠 JSON。模拟数据库适配器／HTTP／浏览器回归、类型检查、Lint 与构建通过。
- 本修复尚未提交或部署，未更改生产开关、凭据、预算、权限或候选记录。上线后需保持开关关闭，重新核验列表及上述固定链接；不能把当前本地测试写成线上问题已经解决。
