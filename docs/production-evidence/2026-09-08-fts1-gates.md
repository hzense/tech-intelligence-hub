# FTS-1 online rollout gate check — 2026-09-08

## Outcome

The operator selected the FTS-1 rollout-gate work. The online prerequisites were
checked, but the gate is **not closed**. After the operator saved the connection
secrets, read-only preflight run #34212653428 completed successfully after manual
Environment approval. The hosted runner connected to production for preflight;
no production database connection was made from this workstation, and no
migration, backfill, ACL mutation, credential rotation or Vercel configuration
change was performed.

## Current verified evidence

- GitHub's `main` reference is `333245f878b31295f291975828fd7937324e6640`,
  the PR #48 merge commit. Its [main CI](https://github.com/hzense/tech-intelligence-hub/actions/runs/34141929106)
  completed successfully. The initial local worktree was clean on `main`.
- The `production-maintenance` Environment still requires review by `zhenghu`,
  disallows admin bypass and has exactly one deployment branch policy:
  `main`, type `branch`. Self-review remains allowed; this is not independent
  two-person approval.
- The Environment has eight non-secret target Variables. Initially its secrets
  list was empty. After the operator reported saving credentials, a fresh query
  confirmed `DATABASE_DIRECT_URL` and `HZENSE_RUNTIME_DATABASE_URL` names exist.
  Only metadata/names were requested; no secret value was read, exported or saved.
- After rechecking current main and successful CI, read-only `preflight`
  [run #34212653428](https://github.com/hzense/tech-intelligence-hub/actions/runs/34212653428)
  was dispatched on `333245f878b31295f291975828fd7937324e6640`.
  After the operator reported manual completion, the run and maintenance job
  were independently verified as `completed/success`, finishing at
  `2026-09-08T09:58:31Z`. All steps succeeded, including both freshness checks
  and bounded execution. The sanitized execution result was
  `{"operation":"preflight","status":"succeeded","pendingMigrationCount":1}`.
  This validates Migrator direct authentication and the preflight contract;
  it does not validate the separately injected Runtime credential.
  No approval was submitted automatically and no production write operation
  was dispatched.
- The public `https://hzense.com/api/health/database` request succeeded with
  `{"status":"ok"}`. This is an application-health observation, not evidence
  of migration `0003`, search backfill, search mode, complete ACLs or restore
  capability. The deployed commit was not independently inspected here.
- Browser access initially failed (`native pipe startup failed`), but the later
  retry connected to Chrome and authenticated Neon Console. Read-only provider
  inspection confirmed a six-hour main history window, one existing historical
  snapshot with no expiry, and a full snapshot quota. The project lists three
  of ten allowed branches; the existing FTS backup branch is still present.
  Its expiration form shows `2026-09-13 23:26 Europe/Berlin (GMT+02:00)`.
  The form was cancelled without saving. No restore, branch creation, deletion,
  plan upgrade, password reset, SQL execution or ACL capture was performed.
  The Console showed the backup compute become active during page inspection;
  this does not establish data parity or restore success.
- A metadata-only GitHub query returned no private repositories visible to the
  current account under the HZense organization; this does not prove none exist
  outside its access. The application repository remains public. No new private
  destination or access grant was created without the operator's choice.

## Open gates

1. **Runtime credential validation:** both required connection Secret names are
   present and Migrator preflight passed. Runtime authentication and its full
   schema/ACL contract remain unverified in this workflow. The current Runtime
   preflight expects the FTS search columns and permissions, so it is not being
   dispatched as a substitute for the pending migration and ACL prerequisites.
   Follow the [online runbook](../ONLINE_MAINTENANCE.md); do not paste credentials
   into chat or create a local env file.
2. **Archive implementation and execution:** after rejecting a private repository,
   the operator explicitly approved plaintext ACL baselines and credential-free
   restoration material in the current public repository, after disclosure of the
   public visibility and long-term retention risk. The destination decision is
   resolved; see [the scoped decision](./acl/README.md). Passwords, tokens, connection
   configuration, raw backup IDs and business data remain excluded. Hosted
   `acl-capture` double-capture and exact-file artifact support have been added in
   the worktree, not yet merged or executed. Public logs remain summary-only.
   Artifact review and durable repository archival are still pending.
3. **Provider backup and recovery:** the Console now confirms the existing FTS
   backup branch and its expiry of 2026-09-13 23:26 Europe/Berlin. Existence and
   expiry do not prove freshness, DDL parity or recoverability. An independent
   restore check and a fresh verified backup for the reviewed maintenance window
   are still required. Do not replace or delete older backups merely to clear
   the gate; the snapshot quota is full, while the branch list reports 3/10.
4. **ACL recovery evidence:** establish the DDL freeze, independently capture
   the current full baseline twice against the same verified backup, review
   the restoration plan and rehearse on an isolated restore. Resolve the
   historical evidence gap or obtain the separately documented operator
   decision required by the existing runbook. No approval declaration was
   fabricated or marked complete in this checkpoint.

## Resume order

Worktree validation for the public archive implementation: database package
tests passed (263 passed, 35 isolated-integration tests skipped); workflow
contract validation, actionlint, ESLint, formatting and `git diff --check`
passed. These are development checks, not production capture or restore evidence.

Read-only preflight and its sanitized outcome are now verified. Its success
must not be treated as proof of recoverability or permission to bypass write
gates. Subsequent runs still require fresh main/CI checks and operator approval.

Provider browser access and the archive destination decision are now available.
Review and merge the hosted capture implementation, independently validate a
fresh backup, freeze DDL, then approve and execute capture. Review and archive
the resulting attachment before completing the restore-plan review and isolated
rehearsal. Only then prepare a run-bound write
approval and follow the existing migration → verification → reviewed search
backfill → Runtime ACL verification → shadow → cutover acceptance sequence.
This checkpoint does not authorize skipping any step or restoring local tools.
