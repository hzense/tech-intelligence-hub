# 四项信息人工确认发布

2026-09-25，本地实现及验证完成，尚未提交或生产启用。

## 使用方式

从 AI 生成任务打开候选后，核对标题、摘要以及四项信息：

1. 事件日期（不是采集日期）。
2. 至少一个组织名称。
3. 至少一个人物姓名。
4. 至少一个启用领域，可输入名称检索并选择已有领域，最多五项。

系统尽量沿用原候选、已有补全及补证结果；缺项直接手填。四项齐全后点击一次“确认发布”即公开，不再要求六项声明、实体登记、独立签名核验和受限发布组装。未补齐可“保存补充”。已发布可以确认更新或撤回。

发布依据明确为“管理员确认”，不标记为独立事实核验通过；不编造组织类型、人物身份、置信度、热度或趋势分数。人物／组织此阶段是人工确认的名称，不冒充已核验实体关系。原始候选与证据折叠辅助查看，不会自动公开原文件、原文快照、导入地址或私有任务编号。标题和摘要仍取原候选，不在这次表单中编辑。

## 保存与公开读取

- `editorial_signal_revisions` 保存追加式 draft / publish / withdraw 修订，原 AI 结果和账本不变。
- `editorial_public_signals` 仅投影每个候选最新修订中已发布的记录，不暴露 owner、原任务编号、证据和审计元数据。
- 正式信号列表、详情和搜索叠加人工发布条目；撤回直接从实时视图消失，旧搜索索引不能让内容重新出现。
- 日期和名称格式、至少一个人物／组织／领域、实时领域目录、管理员归属、材料指纹、确认动作、请求幂等和修订并发控制均由服务端检查。
- 网络超时不等于保存失败；页面保留原请求，先核对状态或重试原编号。重复点击不会追加重复修订。
- 已发布候选必须先撤回再删除原生成任务，避免失去撤回入口。

## 生产启用（尚未执行）

本批新增 `0025_editorial_signal_publication.sql`。先审核迁移和最小授权、核对备份及维护窗口，再经明确批准执行；不要仅部署页面就宣布可用。

1. 先为受保护维护流程补充并审核 0025 专属目标／备份／计划绑定范围，旧 0024 审批不能执行新迁移；随后预检，经批准执行 0025 并完整核验。此批尚未扩展生产维护授权范围。
2. 审核并执行 `db/roles/create_editorial_roles.sql`、`configure_editorial_roles.sql`，为两个独立角色安全设置密码。它们不会扩展原 runtime、generation 或旧发布角色权限。
3. 配置 `HZENSE_EDITORIAL_DATABASE_URL`（`hzense_editorial_writer`）和 `HZENSE_EDITORIAL_READER_DATABASE_URL`（`hzense_editorial_reader`）；仅服务端使用，不写入 Git 或浏览器。
4. 连接须满足现有数据库端点／数据库名及 TLS 限制；验证读写角色精确 ACL 后开启 `HZENSE_EDITORIAL_PUBLICATION_ENABLED=1` 并部署。
5. 经明确内容授权完成一条发布、刷新、列表／详情／搜索和撤回验收。本地合成测试不等于生产验收。

开关默认关闭。关闭会停止新人工发布及其公开读取，但保留修订、原生成、旧 qualified 发布数据和旧只读网站内容。需要仅暂停写入而保留现有公开内容时，不应直接使用此总开关。旧签名核验协议并未降级或伪造。

开关已加入 Turbo 构建缓存键；开启后 Signal 列表／详情／搜索读取动态发布状态，不复用关闭时的静态内容。环境修改后必须重新部署。历史签名核验记录的管理入口保留在候选的“审核材料标识”折叠区，不属于新发布的必填步骤。

### 数据库权限边界

pgvector 例外使用仓库固定的 0.8.6 函数清单（`editorial-vector-manifest.mjs`）：核对 118 项函数的名称、参数／返回类型、语言、库与实现符号及执行属性，同时限制版本、所有权、安全模式和 ACL。清单来自隔离 PostgreSQL 18 的全新安装，不从生产库动态学习；等量替换扩展成员也会拒绝。升级扩展须重新审核清单。

专用角色校验覆盖应用数据库精确表／列 ACL、成员资格、对象所有权、参数 ACL，以及其他数据库的直接授权和非保留库的有效访问。它沿用项目现有的 Neon 平台信任边界：仅允许 `cloud_admin` 所有的 `postgres`／`template1` 满足精确默认数据库 ACL 形态时保留平台访问；这不是对整个 PostgreSQL 实例的绝对隔离保证。

应用不得在这些提供商保留库中存放业务数据或创建可供 PUBLIC 访问的业务对象。当前连接无法跨库检查保留库内部的对象 ACL，因此不能将此校验报告为保留库对象审计已通过。若平台保留库承载了自定义对象，必须先另行审计并消除暴露，不能直接启用本功能。本批不修改提供商保留库或其系统 ACL。

## 本地验证

- Web 默认测试 677 项通过，12 项需显式环境的测试跳过。
- 显式运行候选浏览器回归 3 项通过：含手填四项、一次发布、刷新、更新、撤回、同编号重试、历史回执不误报当前发布、失效领域移除和挂起请求超时恢复；桌面及 390px 布局已检查。
- 数据库普通套件 2330 项通过；独立 PostgreSQL 中新发布持久化、读写权限、追加式历史、撤回和发布／删除并发均通过。完整 fresh／upgrade 迁移核验达到代码目标 26 migrations、58 tables；迁移测试分组全部完成（修正本地环境及旧计数断言后分段续跑）。
- Web／数据库类型检查、改动 lint、格式和 diff 检查通过。
- 关闭和开启人工发布开关的 Next.js 生产构建通过；Turbo dry-run 确认开关取值变化使 Web build hash 变化。
- 独立代码复审发现的历史回执、失效领域、挂起请求三项问题已修复并加入浏览器回归。未调用真实 AI、未发布真实候选、未改生产数据库或环境。
