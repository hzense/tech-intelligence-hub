# ADR 0002 — PostgreSQL + pgvector for structured intelligence

Status: Accepted.

PostgreSQL owns entities, relations, signals, radar state and operational indexes. pgvector keeps semantic retrieval in the same operational system during V1/V2, avoiding a premature standalone vector database.
